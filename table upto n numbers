#include <stdio.h>

int main() {
    // here we give upto whatvalues of table print
    for(int n=1;n<=10;n++){
        for(int i=1;i<=10;i++)
        printf("%d*%d=%d\n",n,i,n*i);
        // we \n for print table numbers in new line
        printf("\n");
    }

    return 0;
}
