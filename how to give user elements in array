#include <iostream>
int main() {
    int n;
    std::cin >> n;
    int i, arr[n];
    for (i = 0; i < n; i++) {
        std::cin >> arr[i];
    }
    for (i = 0; i < n; i++) {
        std::cout << arr[i] << " ";
    }
    return 0;
}
